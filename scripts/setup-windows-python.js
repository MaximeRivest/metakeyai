#!/usr/bin/env node

/*
 * DEPRECATED: This script is no longer used.
 * 
 * MetaKeyAI now uses UV for unified Python environment management
 * across all platforms including Windows. Python setup is handled
 * automatically during first-run setup.
 * 
 * For manual Python setup, use: npm run setup:python
 * For fresh builds, use: npm run clean
 */

console.log('⚠️  DEPRECATED: Windows-specific Python setup is no longer needed');
console.log('📦 MetaKeyAI now uses UV for cross-platform Python management');
console.log('🔧 Run "npm run setup:python" if you need to setup Python manually');
console.log('🧹 Run "npm run clean" for a fresh build');

process.exit(0); 